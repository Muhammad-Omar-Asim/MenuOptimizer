{
  "name": "menu-analyzer",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/index.html && cp -r lib public/ && cd reviewer && npm install && npm run build"
  }
}
