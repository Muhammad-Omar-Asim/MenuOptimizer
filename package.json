{
  "name": "menu-analyzer",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "cd reviewer && npm install && npm run build && cd .. && mkdir -p public/reviewer && cp -r reviewer/dist/* public/reviewer/"
  }
}
